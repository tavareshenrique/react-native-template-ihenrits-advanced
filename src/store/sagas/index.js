import { all, takeLatest } from "redux-saga/effects";

// import { Types as YourTypes } from '../ducks/yourducks';
// import { YourFuncSaga } from './yoursaga';

export default function* rootSaga() {
  // yield all([takeLatest(UsersTypes.ADD_REQUEST, YourFuncSaga)]);
}
